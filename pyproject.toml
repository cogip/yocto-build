[project]
name = "cogip-yocto"
version = "0.1.0"
description = "Cogip Yocto kiosk image build environment (kas wrapper)"
requires-python = ">=3.10"
dependencies = [
    "kas>=4.0",
]

[tool.uv]
package = false
